# Agents

The formal Agent boundary contains `RecordedAction`, `AgentRun`, and the bounded `run_agent_loop()`. Prompts, response parsers, public-control allowlists, and terminal predicates remain scenario-owned.
